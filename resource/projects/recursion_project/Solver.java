import java.awt.Dimension;

public class Solver {
    private Dimension limit;
    private ArrayList<Pair<Pair<Double, Double>, Pair<Double, Double>>> lines;
    public Solver(Dimension screenSize) {
        this.limit = screenSize;
    }

    private ArrayList<Pair<Pair<Double, Double>, Pair<Double, Double>>> enFill(ArrayList<Pair<Pair<?, ?>, Pair<?, ?>>> def) {
        def.forEach(() -> {

        });
    }
}