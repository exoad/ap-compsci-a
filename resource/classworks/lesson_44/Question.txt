1. Which is a more efficient algorithm to write? Why?
2. Which is a more efficient algorithm to run? Why?
3. What are the benefits of writing an algorithm recursively instead of iteratively? What are the drawbacks?
